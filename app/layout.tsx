import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Jeets.Fi',
  description: 'Your Resilient Trading Partner',
  icons: {
    icon: 'https://raw.githubusercontent.com/TnsaAi/jeets-urls-images/refs/heads/main/image1.png'
  },
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}



import './globals.css'